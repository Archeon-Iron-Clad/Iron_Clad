# Copy to .env.local (never commit secrets)
# Local dev: URL from `npx convex dev` (dev deployment)
# Production build: URL from `npx convex deploy --dry-run` (prod deployment — often different host)
VITE_CONVEX_URL=
